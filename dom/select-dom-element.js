"use strict";

// Select a document element by means of the value of the id attribute assigned to it.
const $id = document.getElementById("id");

// Select the first DOM element that matches a CSS selector.
const $class = document.querySelector(".class");
const $elementHtml = document.querySelector("selector");

// Is used to select all DOM elements that match a CSS selector.
const $allClass = document.querySelectorAll(".classes");
const $allElementHtml = document.querySelectorAll("selector");
